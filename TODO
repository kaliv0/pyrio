add descriptions in README
    xml -> read include_root / write custom xml_root
    FileStream.process()
    f_open_opts
    f_read
    f_write

show complex pipeline for combining files
update file (crash mechanism?)

at the end -> flip back tmp_path_retention_policy

