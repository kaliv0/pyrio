- add combiner in reduce -> for parallelized streams

- add description in readme for explicit kwargs (filter_map, sorted)
   NB -> accumulate -> 'func' in signature, 'function' in docs

- write more tests for optional
- add docstrings and types (+mypy)

- add IntegerStream -> evens, odds, math funcs(?) -> or NumStream (support int and float)?

- nth -> rename method -> take_nth?

- Stream:
    - to_number_stream (similar to next one?)
    - map_to_int/float
    - flat_map_to_int/float

- refactor imports
